<div class="container">
    <div class="header">
        <div class="menu">
            <a href="javascript:void(0)"
                onclick="window.location.assign('/inicial')">
                <span></span>
                <span></span>
                <span></span>
            </a>
        </div>
        <div class="col-filter">
            <% if (loggedInUser) { %>
            <p id="name">Bem-vindo, <span><%= loggedInUser %></span></p>
            <% } %>
            <a href="/logout" id="logout"><i class="fas fa-sign-out-alt"></i>
                Logout</a>
        </div>
    </div>

    <section class="pauta">
        <div class="box">
            <p class="pauta-title">🔘 Pauta do dia <span id="minha-data"></span></p>
            <div class="box-table">
                <table class="concluido" id="concluidosTable">
                    <caption>
                        <p>Chamados Concluídos</p>
                    </caption>
                    <thead>
                        <tr>
                            <th scope="col">Chamado</th>
                            <th scope="col">Comentário</th>
                            <th scope="col">Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% pautaActive.filter(db => db.data && new
                        Date(db.data).toISOString().split('T')[0] ===
                        dataAtual).forEach(db => { %>
                        <tr>
                            <th scope="row"><%= db.chamado %></th>
                            <td class="text"><%= db.comentario %></td>
                            <td><%= db.status %></td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
                <table class="pendentes" id="pendentesTable">
                    <caption>
                        <p>Chamados Pendentes</p>
                    </caption>
                    <thead>
                        <tr>
                            <th scope="col">Chamado</th>
                            <th scope="col">Comentário</th>
                            <th scope="col">Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% pautaPausada.filter(data => data.status ===
                        '❗ Pendente').forEach(data => { %>
                        <tr>
                            <th scope="row"><%= data.chamado %></th>
                            <td class="text"><%= data.comentario %></td>
                            <td><%= data.status %></td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    <footer
        style="font-size: 1.2em; padding: 1em; color: #040125; font-family: Arial, Helvetica, sans-serif; font-weight: 700;">
        <p style="text-align: center;">Sistema Criado Por Ihury Ferreira</p>
    </footer>
</div>

<!-- <script defer>
        // Dados de exemplo para gráficos
        const completedData = {
            labels: ['Chamado 1', 'Chamado 2', 'Chamado 3'],
            datasets: [{
                label: 'Chamados Concluídos',
                data: [10, 5, 8],
                backgroundColor: 'rgba(75, 192, 192, 0.2)',
                borderColor: 'rgba(75, 192, 192, 1)',
                borderWidth: 1
            }]
        };
    
        const pendingData = {
            labels: ['Chamado A', 'Chamado B', 'Chamado C'],
            datasets: [{
                label: 'Chamados Pendentes',
                data: [6, 7, 3],
                backgroundColor: 'rgba(255, 99, 132, 0.2)',
                borderColor: 'rgba(255, 99, 132, 1)',
                borderWidth: 1
            }]
        };
    
        // Gráfico de Chamados Concluídos
        const completedCtx = document.getElementById('completedChart').getContext('2d');
        new Chart(completedCtx, {
            type: 'bar',
            data: completedData,
            options: {
                responsive: true,
                scales: {
                    x: {
                        beginAtZero: true
                    },
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });
    
        // Gráfico de Chamados Pendentes
        const pendingCtx = document.getElementById('pendingChart').getContext('2d');
        new Chart(pendingCtx, {
            type: 'bar',
            data: pendingData,
            options: {
                responsive: true,
                scales: {
                    x: {
                        beginAtZero: true
                    },
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });
    </script> -->